version: '3'
services:
  py_postgres:
    container_name: dentist_postgres
    image: 'postgres:11.5'
    ports:
      - '7000:5432'
    volumes:
      - '~/docker/data/postgresql:/var/lib/postgresql/data'
